# Dependências e artefatos de build (recriados no estágio builder)
node_modules
dist
build
coverage
.scannerwork

# Git e IDE
.git
.gitignore
.idea
.vscode
.claude

# Ambiente / segredos (a imagem recebe via variáveis do container)
.env*

# Testes, documentação e configs de dev — fora da imagem de produção
test
docs
k8s
infra
.github
Makefile
*.md
.eslintrc.js
.prettierrc
docker-compose.yml
docker-compose.sonar.yml
sonar-project.properties
*.log

# Mantidos de propósito (a imagem precisa): prisma/, docker-entrypoint.sh,
# package.json, package-lock.json
